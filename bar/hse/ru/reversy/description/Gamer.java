package bar.hse.ru.reversy.description;

public interface Gamer {
    void setScore(int num);

    String getName(); // никогда не думал, что в реплите от порядка может не работать

    int getScore();
}
